/**
 * Public API of the `attendance` module — الحصص المنفذة وسجل الحضور.
 *
 * Other modules may import from this file ONLY. Never deep-import
 * `src/modules/attendance/domain|application|infrastructure|ui` from outside this module.
 * Re-export use cases, queries, domain types and UI components here as they are built.
 */
export {};
